name: FakeBlock
version: ${project.version}-{build.number}
main: pro.husk.fakeblock.FakeBlock
author: husK
description: Enables the creation of fake blocks, so only people with the a permission node can walk through.
commands:
  fakeblock:
    description: Shows all available commands
    usage: /fakeblock
    aliases: [fb]
api-version: 1.13
depend: [ProtocolLib]
softdepend: [Multiverse-Core, LuckPerms]